import { luffyhatCanvas } from '.';

const Hero = () => {
    return (
        <div className="hero">
            <div id="hero" className="section">
                <h1>AARON WARSOP</h1>
                <luffyhatCanvas />
                <p>Soon to be Computer Science graduate looking to further develop my skillset and create a brighter future via the programs we create.</p>
            </div>
        </div>
    );
}

export default Hero;